Switched the default initial ocean temperature profile to the CMIP5 multi-model mean (matching MAGICC7's `CORE_SWITCH_OCN_TEMPPROFILE=2`) with per-hemisphere profiles, and fixed the ground heat coupling to use MAGICC7's explicit formulation instead of absorbing K_LG into the coupled feedback system. Together these reduced parity failures from 11/33 to 1/33, with all ECS sweep values (1.5-4.5) and efficacy tests now passing. Maximum temperature error reduced from ~5% to ~1.2%.
