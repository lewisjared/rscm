Added regional CO2 forcing distribution (qfrac) in the climate solve loop and prescribed efficacy mode (`efficacy_apply=1`). Forcing is now distributed per-box using the CO2 regional pattern, consistent with LAMCALC calibration.
