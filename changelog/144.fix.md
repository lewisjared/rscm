Aligned GHG forcing with its concentration boundary and initialized forcing in the MAGICC runner. Model execution now orders producers before consumers and reports component failures through Python exceptions or Rust's fallible run methods.
