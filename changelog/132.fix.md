Reject nonannual terrestrial carbon timesteps through all solver entry points and include the mass conservation correction in the respiration diagnostic. Model execution stops on component errors: Python raises ValueError, and Rust callers can use try_step/try_run to handle errors without panicking. A failed step may leave partially updated state; restore a checkpoint before retrying.
